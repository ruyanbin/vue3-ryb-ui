import { MagicStringBase } from 'magic-string-ast';
import { FilterPattern } from '@rollup/pluginutils';
export { AttachedScope, attachScopes, normalizePath } from '@rollup/pluginutils';

declare function getTransformResult(s: MagicStringBase | undefined, id: string): {
    code: string;
    map: any;
} | undefined;
interface BaseOptions {
    include?: FilterPattern;
    exclude?: FilterPattern;
    version?: 2 | 3;
}
declare function createFilter(options: BaseOptions): (id: unknown) => boolean;

export { BaseOptions, createFilter, getTransformResult };
